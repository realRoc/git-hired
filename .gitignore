# macOS Finder metadata
.DS_Store

# Local agent workspace (skill source migrated to realRoc/skills)
.agents/
skills/githire

# gstack ship workflow markers
.gstack/

# impeccable design-skill local state (live config + critique snapshots)
.impeccable/

# Build deps for scripts/build-en.mjs (committed: package.json, package-lock.json)
node_modules/
